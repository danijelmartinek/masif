
export interface MtGraphPointType {
    0: number,
    1: number,
    2?: number
};

export interface SvgLineSegmentType {
    length: number,
    angle: number
};

export interface MaxAxesPoints {
    x: number,
    y: number
}

export interface RangeType {
    min: number,
    max: number
}

